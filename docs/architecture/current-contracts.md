# Current Architecture Contracts

Status: active

Date: 2026-07-03

Scope: current target-state contracts for day-to-day WideNote development

This file is the current-state layer between the historical decision log and
implementation work. Coding agents and humans should read the task-relevant
contracts here before changing a module. ADRs and RFCs remain authoritative for
decision history, tradeoffs, and rationale, but this file states what the
project is currently maintaining.

## How To Use

1. Start with this file for the current target state.
2. Use the nearest module README for local ownership and implementation
   boundaries.
3. Open the linked ADRs or RFCs when the contract is unclear, the change would
   alter the contract, or history and rationale matter.
4. Encode important contracts in tests, validators, or documented review gates
   whenever practical.

Do not treat current implementation behavior as authoritative when it conflicts
with a current contract. Fix the implementation or update the contract through
the decision process.

## Maintenance Rules

- When an accepted ADR or RFC changes the target state of a product,
  architecture, runtime, schema, privacy, sync, Agent Pack, plugin-permission,
  technology, or default-UX boundary, update this file in the same change.
- When a module starts implementing a contract listed here, link this file from
  that module README.
- When a contract changes, update or add executable proof: tests, schema
  validation, pack validation, docs lint, integration QA, or an explicit skipped
  check with risk.
- Keep this file short and operational. Put history, rejected alternatives, and
  detailed evidence in ADRs, RFCs, or research notes.
- Do not rewrite ADRs to hide history. Supersede them with new ADRs, then
  update this file to the new current state.

## Contract Index

| Area | Current contract | Applies first to | Enforced by | Provenance |
| --- | --- | --- | --- | --- |
| Product loop | The default loop is quick capture -> timeline/cards -> Memory -> insight. Backend services, advanced exports, broad imports, hosted runners, registries, and community packs enhance the loop; they must not become prerequisites for first use. | `apps/mobile/**`, `packs/official/**`, `docs/product/**` | Mobile journey tests, module README review, product docs review | [ADR-0005](../decisions/0005-use-memory-first-instead-of-pkm-core.md), [ADR-0007](../decisions/0007-defer-cloud-sync-from-core-phase-one.md), [Phase-One Product Scope](../rfcs/phase-one-product-scope.md) |
| Clean-room reference use | Memex, Omi, and other public references may inform WideNote-authored capability specs, interaction patterns, acceptance tests, and research summaries. Do not copy reference code, database schemas, migrations, prompts, private APIs, UI assets, proprietary algorithms, or fixtures. Durable behavior must land in WideNote research, ADRs/RFCs, schemas, module READMEs, and tests. | `docs/research/**`, `docs/rfcs/**`, `docs/decisions/**`, `apps/**`, `packages/**`, `packs/**` | Review for copied implementation/schema/assets, acceptance tests that validate WideNote behavior | [ADR-0006](../decisions/0006-use-clean-room-parity-specs.md) |
| Mobile navigation hierarchy | The only peer bottom-tab route roots are Home `/`, Chat `/chat`, Todos `/todos`, and Plugins `/plugins`. The center Record item is a capture action, not a route root. The bottom navigation bar is visible only on those four roots; every other mobile page renders as an independent child page without bottom tabs. Every child page belongs under an owning parent route and must preserve a system-back path before platform exit. Direct child controls should use push-style navigation; shortcuts that skip an intermediate parent should construct the declared parent stack. Bottom-tab switches use replacement-style navigation. Direct links to non-tab pages must still construct their parent back stack. Contextual source links may preserve the visible source page as the immediate back target, but the destination remains a child page without bottom tabs. | `apps/mobile/lib/app/app_router.dart`, `apps/mobile/lib/features/**/presentation/**` | Navigation hierarchy and bottom-tab visibility widget tests, route review for new pages | [ADR-0015](../decisions/0015-use-hierarchical-mobile-navigation.md), [ADR-0019](../decisions/0019-hide-bottom-tabs-on-mobile-child-pages.md) |
| Local-first ownership | The mobile client owns immediate capture, local persistence, local runtime hosting, permissions, and first-use UX. Optional backend and runner services may sync, schedule, back up, or execute longer work, but they are not the canonical brain. | `apps/mobile/**`, `apps/api/**`, `apps/runner-ts/**`, `packages/dart/**`, `packages/ts/**` | Architecture review, package tests, integration QA | [ADR-0003](../decisions/0003-build-agent-runtime-kernel.md), [ADR-0007](../decisions/0007-defer-cloud-sync-from-core-phase-one.md), [Architecture Overview](./overview.md) |
| Source truth | Original user records and source material are source truth. AI outputs are derived objects and must not overwrite raw input. Derived outputs must preserve source references when they depend on user records. | `apps/mobile/lib/features/capture/**`, `packages/dart/local_db/**`, `packages/dart/agent_runtime/**`, `packages/dart/cards/**`, `packages/dart/memory/**` | Orchestration tests, backup/restore tests, source-ref assertions | [ADR-0009](../decisions/0009-use-object-truth-and-context-packets.md), [Phase-One Umbrella Technical Plan](../rfcs/phase-one-umbrella-technical-plan.md) |
| Capture processing | Quick capture persists the local source record immediately, then queues full model/runtime processing in the background. Processing state must not block additional text, attachment, or voice records. Ready photo attachments may run through the built-in Capture Media Semantic Agent before downstream context construction; that agent requires a Vision-capable model provider, writes source-linked `vision_summary` and `ocr_text` derived artifacts, and fails with a clear media preprocessing reason without overwriting raw input. Pending records are visible in Home and Timeline, failed records preserve raw input and expose per-record retry, and pending records cannot be edited or deleted in this slice. A completed capture may have no Memory when no valid Memory proposal is produced; the local capture payload persists `memory_generated` for this distinction. Default model or capture-agent failure remains a failed capture. Runtime task claim, retry backoff, dependency blocking, and concurrency control are durable local-db behavior. Handler failures default to two attempts, terminal schema/permission/approval failures are not auto-retried, and stale running runs/tasks from interrupted or native-crash-like execution recover through the same retry budget before becoming failed. Short OS background drains may continue already-saved ready captures without taking over recording, location, or long-lived runner work. | `apps/mobile/lib/features/capture/**`, `apps/mobile/lib/features/timeline/**`, `packages/dart/agent_runtime/**`, `packages/dart/local_db/**`, `packages/dart/memory/**` | Capture widget tests, timeline widget tests, orchestration tests, runtime scheduler tests, local DB task tests | [ADR-0017](../decisions/0017-accept-continuous-capture-background-processing.md), [ADR-0018](../decisions/0018-accept-durable-agent-work-queue.md), [Continuous Capture Research](../research/2026-07-02-continuous-capture-background-processing.md) |
| Voice transcription | Voice capture saves raw audio before ASR. New first-slice recordings use WAV. The mobile transcription module owns explicit ASR engine selection, local SenseVoice model state, MiMo ASR configuration, real-time preview, retries, and correction orchestration. Transcripts are `audio_transcript` derived artifacts and downstream outputs must cite capture, attachment, transcript, and event refs. Local SenseVoice and MiMo ASR are explicit user-selected engines, not an automatic fallback chain; manual MiMo retry remains an explicit user action. Existing `.m4a` recordings are not first-slice local-ASR compatibility targets. Local ASR model files are excluded from full backups and must be redownloaded after restore. `audio.transcribe` Runtime exposure waits until it can wrap the mobile transcription engine. | `apps/mobile/lib/features/capture/**`, `apps/mobile/lib/features/transcription/**`, `apps/mobile/lib/features/settings/**`, `packages/schemas/**`, `packages/dart/agent_runtime/**` | Transcript schema fixtures, mobile transcription service/settings tests, backup media tests, runtime wrapper tests when added | [ADR-0012](../decisions/0012-accept-voice-transcription-and-correction-boundaries.md), [ADR-0016](../decisions/0016-restore-ready-logs-backups-and-asr.md), [Voice ASR Plan](../research/2026-06-30-voice-input-asr-correction-plan.md) |
| Location context | Location capture is opt-in foreground GPS metadata for records. AMap reverse geocoding is a separate opt-in because it sends coordinates to a third party. Precise coordinates and geocoded place names are record facts: they live on the local capture payload under `location_context` and the structured fact projection `fact_metadata.location`, not duplicated in runtime event payloads. The coordinate fact is the source coordinate; the AMap place fact is a provider-marked derived place with source refs back to the capture. Timeline UI and future visualizations should read the fact projection while default list display stays coarse/redacted. Reverse-geocode failure must not block capture, and users can stop future capture and clear saved location facts. AMap keys live in secure storage and may enter only the full, secret-bearing `.widenote` backup allowlist; they must not enter Owner Export, safe projections, logs, fixtures, screenshots, external review prompts, PR descriptions, or generated docs. | `apps/mobile/lib/features/location/**`, `apps/mobile/lib/features/capture/**`, `apps/mobile/lib/features/settings/**`, `apps/mobile/lib/features/timeline/**` | Location unit/widget tests, platform permission tests, capture persistence/fact tests, timeline metadata tests, emulator QA when available | [ADR-0014](../decisions/0014-accept-location-capture-boundaries.md), [ADR-0016](../decisions/0016-restore-ready-logs-backups-and-asr.md), [Location Context AMap/Kimi Review](../research/2026-07-01-location-context-amap-kimi-review.md) |
| Todo and schedule admission | Source-linked todos are model-derived suggestions, not a mirror of every capture. Ordinary diary, status, observation, or product-note captures should stay on the timeline without a `wn.todo.suggested` event when the Todo agent returns `quiet`. Model-selected actions and schedules are displayed separately as action items and schedule candidates; schedule candidates are local suggestions and must not write system Calendar or Reminder data until a follow-up permission and UX decision lands. Core must not use local keyword, regular-expression, substring, or stop-word heuristics to decide action/schedule/quiet admission. | `apps/mobile/lib/features/capture/**`, `apps/mobile/lib/features/todos/**`, `packs/official/todo/**` | Capture orchestration tests, todo controller tests, todo widget tests, backup/legacy-row tests | [ADR-0010](../decisions/0010-delegate-semantic-selection-to-models.md), [Todo And Schedule Admission Kimi Review](../research/2026-07-02-todo-schedule-admission-kimi-review.md) |
| Insight depth and review | Insights are source-linked derived state. The current mobile Insights surface reads local `InsightRecord` rows, renders claims, metrics, supporting evidence, counter-evidence, and source refs, and stores archive/restore as local lifecycle evidence. The public Insight Payload schema lives in `packages/schemas/src/insight/`. Deterministic first-pass insight generation may remain lightweight, but deep semantic insight generation must be a future dedicated official `pack.insight_depth` path with public schemas, permissioned tools, valid source refs, runtime task/run attribution, reviewable traces, and model-backed judgment. When the model, retriever, permission, or evidence is unavailable, deep insight generation must fail closed instead of using local keyword heuristics. High-risk, sensitive, conflicting, or low-confidence claims route to review or softening before auto-write. | `apps/mobile/lib/features/insights/**`, `packages/schemas/src/insight/**`, `packages/dart/cards/**`, future `packs/official/insight_depth/**` | Insight schema fixture validation, card payload tests, insights widget tests, future pack/runtime tests | [ADR-0020](../decisions/0020-accept-insight-depth-foundation.md), [Insight Depth Design](../research/2026-07-03-insight-depth-design.md), [ADR-0010](../decisions/0010-delegate-semantic-selection-to-models.md) |
| Memory write policy | Durable, low-risk, source-linked, non-conflicting Memory is auto-accepted by default. Low-confidence, conflicting, highly sensitive, credential-like, or policy-unclear Memory goes to review. Review is the exception path, not the default capture path. | `packages/dart/memory/**`, `apps/mobile/lib/features/capture/**`, `apps/mobile/lib/features/memory/**`, `packs/official/**` | Memory unit tests, capture orchestration tests, long-journey QA | [ADR-0005](../decisions/0005-use-memory-first-instead-of-pkm-core.md), [Memory Model RFC](../rfcs/memory-model.md), [Engineering Rules](./engineering-rules.md#memory-write-policy) |
| Memory visibility and correction | Accepted Memory is visible, editable, deletable through tombstone or revision, source-linked, and reversible enough that auto-accept remains safe. Review surfaces are for exceptions and correction, not confirmation of every capture. | `apps/mobile/lib/features/memory/**`, `packages/dart/memory/**`, `packages/dart/local_db/**` | Memory service tests, Memory page tests, backup/restore tests | [ADR-0005](../decisions/0005-use-memory-first-instead-of-pkm-core.md), [Memory Model RFC](../rfcs/memory-model.md) |
| Object truth and context packets | SQLite/Drift object tables remain the canonical local object truth. Context packets, projections, FTS, vector indexes, recaps, cards, and other read models are derived and rebuildable unless a future ADR says otherwise. User-facing backup exports use a compressed `.widenote` directory archive with staged restore and entry hash verification. The default archive carries a full SQLite snapshot, local capture media files, and allowlisted secure-storage settings/credentials needed for direct-use restore, not JSON or Markdown restore documents. Debug/profile builds and release builds with an explicit non-formal flavor may append `diagnostics/*.log` files and `diagnostics/export-info.txt` for support; these files are checksum-verified but ignored by restore. Formal release flavors and unflavored release builds omit extra diagnostics. Local ASR model binaries stay outside backups. Treat `.widenote` files as secret-bearing local artifacts. | `packages/dart/local_db/**`, `apps/mobile/lib/app/local_database.dart`, `packages/dart/agent_runtime/**`, future context modules | Local DB tests, backup/restore tests, context-packet tests when added | [ADR-0009](../decisions/0009-use-object-truth-and-context-packets.md), [ADR-0012](../decisions/0012-accept-voice-transcription-and-correction-boundaries.md), [ADR-0013](../decisions/0013-preserve-provider-credentials-in-widenote-backups.md), [ADR-0016](../decisions/0016-restore-ready-logs-backups-and-asr.md), [Phase-One Umbrella Technical Plan](../rfcs/phase-one-umbrella-technical-plan.md) |
| Hybrid retrieval and evidence handles | Timeline search and Agent read tools use a hybrid retrieval baseline: BM25/FTS candidates plus dense embedding candidates plus metadata filters, fused with RRF. Search projections and vectors are rebuildable derived state. Embedding provider config is separate from chat/completion model provider config and defaults to an OpenRouter Qwen embedding preset. If no embedding key is configured, product search falls back to keyword/FTS and stays usable. Agent search returns evidence handles and snippets first; follow-up tools open source chunks or list sources rather than injecting full context by default. | `apps/mobile/lib/features/retrieval/**`, `apps/mobile/lib/features/timeline/**`, `apps/mobile/lib/features/chat/**`, `packages/dart/local_db/**`, `packages/dart/model_providers/**` | Search index tests, core tool catalog tests, timeline search widget tests, embedding provider tests | [Hybrid Retrieval Review](../research/2026-07-03-hybrid-retrieval-kimi-review.md), [ADR-0009](../decisions/0009-use-object-truth-and-context-packets.md), [ADR-0010](../decisions/0010-delegate-semantic-selection-to-models.md) |
| Public contracts | Public Event, Transcript, Memory, Agent Pack, Marketplace Index, Permission, Trace, Backup, Sync, Tool, Task, UI Block, and Pack UI Contribution contracts belong in `packages/schemas` unless an accepted ADR says otherwise. Feature modules must not define private copies of public contracts. | `packages/schemas/**`, `apps/mobile/**`, `packages/dart/**`, `packages/ts/**`, `packs/**` | Schema fixture validation, pack validator, module README review | [Agent Pack Schema RFC](../rfcs/agent-pack-schema.md), [ADR-0012](../decisions/0012-accept-voice-transcription-and-correction-boundaries.md), [ADR-0021](../decisions/0021-use-host-rendered-pack-ui-contributions.md), [Phase-One Umbrella Technical Plan](../rfcs/phase-one-umbrella-technical-plan.md) |
| Agent Packs, marketplace, and permissions | Agent Packs depend on public schemas and SDK boundaries. The phase-one marketplace is GitHub-first: manifests carry marketplace metadata, `packs/marketplace/index.json` is the bundled catalog, and mobile Pack Library displays installed/bundled metadata before remote install exists. Additive slots may extend flows; replacement slots are reserved for `official` or `local_dev` packs until permission, rollback, and review gates are accepted. Pack UI contributions are manifest-declared, host-rendered entries for public app surfaces such as settings, pack home panels, result details, timeline accessories, chat tool results, and future page slots; store-safe/community contributions are data and schema, not arbitrary Flutter, WebView, or native handler code. A declared surface becomes user-visible only after the mobile host implements that renderer, permission gate, and widget coverage; this slice renders Pack Library metadata and permission-gated Settings entries, while detail `event_blocks` renderers remain follow-up work. Navigation-level UI such as bottom tabs is reserved for `official` or `local_dev` packs until arbitration, rollback, and conflict policy is accepted. Subscription `depends_on` may target same-pack subscriptions or fully-qualified external subscriptions; dependency failure blocks downstream work. Sensitive or high-risk tools need explicit permissions, reviewable traces, and local-dev or community-edition gates where needed. Official mobile native handlers are an app-owned trusted built-in path, not a community extension mechanism: they may project to mobile-private state only when the official pack is enabled, required permission grants are present, outputs match declared `output_events`, required `source_refs` are valid, and the output event is attributed to a succeeded runtime task/run for the matching official pack and agent. Local traces may persist raw prompts, model responses, tool inputs, and tool results for review in Agent Console; safe exports/external review/log paths must not reuse those raw payloads. Community packs must use public schemas and permissioned tools/events; they must not write mobile-private tables directly or register native handlers through the official bridge. | `packs/**`, `packages/dart/agent_runtime/**`, `packages/ts/agent_sdk/**`, `apps/mobile/**` | Pack validator, runtime permission tests, Pack Library widget tests, trace tests | [ADR-0011](../decisions/0011-adopt-agent-runtime-roadmap.md), [ADR-0016](../decisions/0016-restore-ready-logs-backups-and-asr.md), [ADR-0018](../decisions/0018-accept-durable-agent-work-queue.md), [ADR-0021](../decisions/0021-use-host-rendered-pack-ui-contributions.md), [Agent Pack Schema RFC](../rfcs/agent-pack-schema.md), [Agent Runtime Capability Boundaries](../rfcs/agent-runtime-capability-boundaries.md), [Marketplace PKM Plan](../research/2026-06-28-marketplace-pkm-plan.md) |
| Model provider access | Model access is local-first BYOK configuration. Mobile Settings provides editable presets for mainstream OpenAI-compatible and Anthropic-compatible services, including OpenAI Chat Completions, OpenAI Responses, Anthropic Claude, Google Gemini, OpenRouter, DeepSeek, Kimi, Qwen, Doubao, Zhipu GLM, MiniMax, MIMO, Ollama, and custom compatible endpoints. Provider configs preserve an access mode (`api_key`, `token_plan`, `coding_plan`, or `local`) so domestic subscription endpoints stay distinguishable from pay-as-you-go API keys. Token Plan and Coding Plan presets remain editable because official base URLs can vary by account, region, or plan. Embedding provider configs are a separate retrieval-owned configuration surface and must not be collapsed into chat/completion provider records. The built-in DeepSeek preset uses the Anthropic-compatible `/anthropic` API with `deepseek-v4-flash`; OpenAI-compatible DeepSeek gateways should use a custom OpenAI-compatible provider. The model picker fetches official provider model lists on demand, uses a dropdown for selection, keeps a custom model-id fallback, and shows conservative Text-only, Vision, Audio, and Video capability tags inferred from provider/model names with explicit user override checkboxes. Product runtime keeps text/chat routing on the saved default provider while media semantic work asks for a Vision-capable provider, preferring the default provider when it has Vision and otherwise the first active Vision provider. Add/edit dialogs provide a user-initiated draft connection test before save; draft test state is not durable configuration. Product runtime reads saved settings only; QA dart-defines remain test harness overrides. Default `.widenote` backups preserve provider credential values for direct-use restore; legacy JSON/Markdown projections remain no-secret surfaces. | `packages/dart/model_providers/**`, `apps/mobile/lib/features/model_providers/**`, `apps/mobile/lib/features/retrieval/**`, `packages/dart/local_db/**` | Model provider package tests, provider settings widget tests, embedding provider tests, backup secret-boundary tests | [ADR-0013](../decisions/0013-preserve-provider-credentials-in-widenote-backups.md), [ADR-0016](../decisions/0016-restore-ready-logs-backups-and-asr.md), [Model Provider Settings RFC](../rfcs/model-provider-settings.md), [Phase-One Product Scope](../rfcs/phase-one-product-scope.md), [Model Provider Token Plan Parity](../research/2026-07-03-model-provider-token-plan-parity.md), [Hybrid Retrieval Review](../research/2026-07-03-hybrid-retrieval-kimi-review.md) |
| Transcript correction Pack | Transcript correction is an additive Agent Pack capability. The official correction pack reads transcript text plus permissioned Memory or glossary context and emits source-linked `wn.transcript.corrected`. In the current short-term mobile persistence model, a correction revision is the `audio_transcript` derived artifact payload plus the `wn.transcript.corrected` event; do not add a dedicated `transcript_correction_revisions` table until audit, undo, or historical review requirements are accepted. It must not read raw audio or write Memory directly. High-confidence exact term/name corrections auto-apply by default; medium/low confidence or meaning-, number-, date-, credential-, medical-, legal-, or financial-affecting patches route to review. | `packs/official/transcript_correction/**`, `apps/mobile/lib/features/transcription/**`, `apps/mobile/lib/features/capture/**`, `packages/schemas/src/transcript/**`, `packages/dart/agent_runtime/**` | Pack validator, transcript schema fixtures, mobile transcription/correction tests, capture orchestration tests | [ADR-0012](../decisions/0012-accept-voice-transcription-and-correction-boundaries.md), [Agent Pack Schema RFC](../rfcs/agent-pack-schema.md) |
| Semantic selection | Core semantic decisions use governed model/context boundaries, not local keyword heuristics. Local deterministic checks are allowed for safety, permissions, schema validation, and exact technical parsing. | `apps/mobile/**`, `packages/dart/**`, `packs/**` | Targeted tests for model/context routing, audits for keyword heuristics | [ADR-0010](../decisions/0010-delegate-semantic-selection-to-models.md) |
| Documentation context | Agents should load context progressively: root instructions, operational principles when a cross-cutting trigger matches, task-relevant current contracts, project map when routing is unclear, nearest README, then decision index and related ADR/RFC only when changing a contract or needing rationale. `START_HERE.md` is for orientation, not required pre-work for every task. `operational-principles.md` is a preflight trigger list; this file is the current-state contract layer; ADRs are decision history. | `AGENTS.md`, `docs/agent-context/**`, `docs/architecture/**`, module READMEs | Docs-only link checks, project-map review | [ADR-0000](../decisions/0000-use-decision-records.md), [ADR-0004](../decisions/0004-use-progressive-context-structure.md) |

## Module Routing Hints

| If touching | Read these current contracts first | Then read |
| --- | --- | --- |
| `apps/mobile/lib/app/app_router.dart` or mobile page navigation | Mobile navigation hierarchy, Product loop, local-first ownership | App README, nearest feature README, ADR-0015 |
| `apps/mobile/lib/features/capture/**` | Product loop, source truth, Memory write policy, semantic selection | Capture README, Memory Model RFC, ADR-0005, ADR-0009 |
| `apps/mobile/lib/features/insights/**` | Insight depth and review, source truth, semantic selection, mobile navigation hierarchy | Insights feature README, ADR-0020, Insight Depth Design |
| `apps/mobile/lib/features/transcription/**`, voice capture transcription, or transcript correction paths | Source truth, voice transcription, transcript correction Pack, public contracts, object truth and context packets | Transcription feature README, Capture media README when raw recording changes, ADR-0012, ADR-0016, Voice ASR Plan, Schemas README, Agent Pack Schema RFC |
| `apps/mobile/lib/features/location/**` | Location context, source truth, local-first ownership | Location README, ADR-0014, Privacy Model |
| `apps/mobile/lib/features/todos/**` | Todo and schedule admission, source truth, semantic selection | Todos feature README, Todo Pack README, Todo Schedule Admission Kimi Review |
| `apps/mobile/lib/features/memory/**` | Memory write policy, Memory visibility and correction, source truth | Memory feature README, Memory package README, Memory Model RFC |
| `apps/mobile/lib/features/retrieval/**`, timeline search, or Agent search tools | Hybrid retrieval and evidence handles, object truth and context packets, model provider access, source truth | Retrieval feature README, Local DB README, Model Providers README, Hybrid Retrieval Review |
| `packages/dart/memory/**` | Memory write policy, Memory visibility and correction, source truth | Memory package README, ADR-0005, Memory Model RFC |
| `packages/dart/local_db/**` or backup/restore paths | Source truth, object truth and context packets, Hybrid retrieval and evidence handles, Memory visibility and correction | Local DB README, ADR-0009, backup RFC/research |
| `packages/dart/agent_runtime/**` or `packs/**` | Local-first ownership, Agent Packs, marketplace, and permissions, public contracts, source truth | Runtime README, pack README, ADR-0011, Agent Pack Schema RFC |
| `packages/dart/model_providers/**`, `apps/mobile/lib/features/model_providers/**`, or embedding provider code | Model provider access, Hybrid retrieval and evidence handles, local-first ownership, source truth | Model Providers README, Mobile model providers README, Retrieval feature README, Model Provider Settings RFC |
| `packages/schemas/**` | Public contracts, source truth, Agent Packs and permissions | Schemas README, schema fixtures, Agent Pack Schema RFC |
| `packages/schemas/src/insight/**` | Insight depth and review, public contracts, source truth | Schemas README, ADR-0020, Insight Depth Design |
| `docs/decisions/**`, `docs/rfcs/**`, `docs/architecture/**`, `docs/agent-context/**` | Documentation context and any contract area being changed | Decision update protocol, docs README, relevant ADR/RFC |

### Mobile Navigation Maintenance

When a mobile page route is added, moved, or renamed, update the GoRouter route
tree, `mobileParentPathFor` fallback coverage, `apps/mobile/lib/app/README.md`
route inventory, and `apps/mobile/test/navigation_hierarchy_test.dart` parent
stack/bottom-tab cases in the same change.
